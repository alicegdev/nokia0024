import { StyleSheet } from "react-native";

// import { BackgroundColor, spacing } from "src/styles";

export default StyleSheet.create({
  root: {
    flex: 1,
    // backgroundColor: BackgroundColor.grey,
    // padding: spacing.md,
  },
  notFirstChild: {
    // marginTop: spacing.md,
  },
});
