# ## Conf http
# server {
#     listen 80;
#     listen [::]:80;
#     server_name n0kia-0024.com;  # Remplacez par votre nom de domaine ou adresse IP

#     location /socket.io/ {
#         #proxy_pass http://51.158.69.60:5050/socket.io/;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "Upgrade";
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_cache_bypass $http_upgrade;
#     }

#     location / {
#         #proxy_pass http://api:5050/;
#         return 301 https://n0kia-0024.com$request_uri;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_cache_bypass $http_upgrade;
#     }
# }

server {
    listen 80;
    listen [::]:80;

    server_name n0kia-0024.com;
    server_tokens off;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        #return 301 https://n0kia-0024.com$request_uri;
        proxy_pass http://api:5050;
        # # Proxy WebSocket
        # proxy_http_version 1.1;
        # proxy_set_header Upgrade $http_upgrade;
        # proxy_set_header Connection "Upgrade";

        # # Autres en-têtes importants
        # proxy_set_header Host $host;
        # proxy_set_header X-Real-IP $remote_addr;
        # proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        # proxy_set_header X-Forwarded-Proto $scheme;

        # # Timeout pour WebSocket (pour éviter la fermeture prématurée des connexions)
        # proxy_read_timeout 60s;
        # proxy_send_timeout 60s;
    }
}

# server {
#     listen 443 ssl http2 default_server;
#     listen [::]:443 ssl http2;

#     server_name n0kia-0024.com;

#     ssl_certificate /etc/nginx/ssl/live/n0kia-0024.com/fullchain.pem;
#     ssl_certificate_key /etc/nginx/ssl/live/n0kia-0024.com/privkey.pem;

#     location / {
#        proxy_pass http://api:5050;
#     }
# }
